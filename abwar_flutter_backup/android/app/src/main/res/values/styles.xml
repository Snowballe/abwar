<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de lancement pour ABWAR -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Configuration de la barre de statut -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@color/background_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Configuration de la barre de navigation -->
        <item name="android:navigationBarColor">@color/background_primary</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Configuration de la fenêtre -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
    
    <!-- Thème normal pour ABWAR -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Configuration de la barre de statut -->
        <item name="android:statusBarColor">@color/background_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Configuration de la barre de navigation -->
        <item name="android:navigationBarColor">@color/background_primary</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Configuration de la fenêtre -->
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
    
    <!-- Thème de lancement sombre pour ABWAR -->
    <style name="LaunchTheme.Dark" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Configuration de la barre de statut -->
        <item name="android:windowBackground">@drawable/launch_background_dark</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Configuration de la barre de navigation -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Configuration de la fenêtre -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
    
    <!-- Thème normal sombre pour ABWAR -->
    <style name="NormalTheme.Dark" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Configuration de la barre de statut -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Configuration de la barre de navigation -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Configuration de la fenêtre -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
